import { describe, it, expect } from "vitest";

// Placeholder suite to keep CI green until real cron tests are added
describe("cron placeholder", () => {
	it("runs", () => {
		expect(true).toBe(true);
	});
});

